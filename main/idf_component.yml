## IDF Component Manager Manifest File
dependencies:
  esp_jpeg:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=5.0"
  espressif/mqtt:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=6.0"
